# strawberry-github-app
Scaffold for GitHub App used by Strawberry orchestration for MOI Batched project.

This repo holds:
- `server/` webhook receiver (Node.js/Express sample)
- `.github/app-manifest.yml` manifest to create GitHub App
- CI workflows to build and publish container

Local developer: antigravity agent created this commit. DO NOT publish the private key to repo.